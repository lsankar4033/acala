// TODO: Fill out + put in a utils-like file
struct Transaction {}

pub struct TxPool {}

impl TxPool {
    // TODO:
    fn insert(tx: Transaction) {}

    // TODO:
    fn popBatch(size: u32) -> [Transaction] {
        []
    }
}
